interface:
  display_name: "Vet"
  short_description: "Vet implementation-audit findings"
  default_prompt: "Use $vet to review another assistant's audit report; the workflow discovers the input and resolves the output path."

policy:
  allow_implicit_invocation: false
