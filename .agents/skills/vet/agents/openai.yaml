interface:
  display_name: "Vet"
  short_description: "Vet implementation-audit findings"
  default_prompt: "Use $vet <audit-report> to vet another assistant's implementation-audit findings."

policy:
  allow_implicit_invocation: false
