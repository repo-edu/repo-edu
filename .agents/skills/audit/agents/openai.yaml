interface:
  display_name: "Audit"
  short_description: "Audit a plan implementation"
  default_prompt: "Use $audit <plan-file> to run its next plan-owned implementation-audit batch."

policy:
  allow_implicit_invocation: false
