interface:
  display_name: "Brief"
  short_description: "Retell an implementation-audit round in plain words"
  default_prompt: "Use $brief to retell the newest *-round.md transcript by its recorded start time, or $brief <transcript-file> to name one."

policy:
  allow_implicit_invocation: false
