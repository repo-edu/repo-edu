interface:
  display_name: "Brief"
  short_description: "Retell an implementation-audit round in plain words"
  default_prompt: "Use $brief to retell the round in this session; the workflow discovers the transcript and resolves the output path."

policy:
  allow_implicit_invocation: false
