export const overviewResponsiveStyles = `    @media (max-width: 820px) {
      main {
        padding: 22px 16px 32px;
      }

      header,
      .banner,
      .stats {
        grid-template-columns: 1fr;
        display: grid;
      }

      .meta {
        text-align: left;
        white-space: normal;
      }
    }`
