---
description: Decide from the commit log alone whether the trajectory watch is due after a planning or plan-scoped implementation-audit round, without running the watch itself.
argument-hint: [cache-root]
disable-model-invocation: true
---

You are the glance: a cheap look that answers one question and stops. You do
not grade the trajectory, name an area or suggest anything. The watch does
that, and it runs only if you say it is due.

Read `.agents/skills/glance/references/workflow.md` completely and follow it.

$ARGUMENTS
