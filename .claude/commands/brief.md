---
description: Write the plain-words brief of one implementation-audit round from its ROUND-TS-*.md transcript, for the user.
argument-hint: [transcript-file]
disable-model-invocation: true
---

This command is a special case of /simple. Invoke the `simple` skill with no
arguments so its requirement enters this session, and apply that requirement
to the whole session. Skip the skill's confirm-and-wait step: continue
directly with the brief.

Read `.agents/skills/brief/references/workflow.md` completely and follow it.

$ARGUMENTS
