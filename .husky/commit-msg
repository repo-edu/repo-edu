# Stamps the commit's capability tag and model record and refuses a subject the
# grammar in .agents/references/subject-grammar.md does not admit. The parser
# in tools/audit-round/src/subject.ts is the grammar's one reader.
node --import tsx tools/audit-round/src/commit-msg-main.ts repo-edu "$1"
