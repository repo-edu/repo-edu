{
  "name": "@repo-edu/tree-sitter-grammar-assets",
  "private": true,
  "type": "module",
  "dependencies": {
    "@repo-edu/domain": "workspace:*"
  },
  "devDependencies": {
    "tree-sitter-cli": "0.26.11"
  },
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "copy-assets": "tsx scripts/copy-assets.ts dist",
    "build": "tsc -p tsconfig.json && pnpm run copy-assets",
    "typecheck": "tsc -p tsconfig.typecheck.json --noEmit && tsx scripts/validate-manifest.ts",
    "check": "pnpm run typecheck",
    "test": "pnpm run copy-assets && node --import tsx --test src/**/*.test.ts"
  }
}
