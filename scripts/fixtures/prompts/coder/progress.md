At the very end of your reply, add one line on its own — exactly
`PROGRESS: in-progress` if there's meaningful work still to do on this
assignment, or `PROGRESS: done` if the assignment is substantively
complete. Be honest; `done` should mean the code actually delivers what
the assignment asks for.
